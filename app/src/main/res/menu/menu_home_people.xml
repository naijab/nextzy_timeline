<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/show_in"
        android:title="@string/show_in"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/show_in_list"
                android:title="@string/show_in_list"
                app:showAsAction="never" />

            <item
                android:id="@+id/show_to_grid"
                android:title="@string/show_in_grid"
                app:showAsAction="never" />
        </menu>
    </item>

    <item
        android:id="@+id/short_by_latest"
        android:title="@string/short_by_latest"
        app:showAsAction="never" />

    <item
        android:id="@+id/short_by_name"
        android:title="@string/short_by_name"
        app:showAsAction="never" />

    <item
        android:id="@+id/short_by_position"
        android:title="@string/short_by_position"
        app:showAsAction="never" />

</menu>