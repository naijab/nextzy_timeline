<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/short_by_latest"
        android:title="@string/short_by_latest"
        app:showAsAction="never" />

    <item
        android:id="@+id/short_by_name"
        android:title="@string/short_by_name"
        app:showAsAction="never" />

    <item
        android:id="@+id/short_by_position"
        android:title="@string/short_by_position"
        app:showAsAction="never" />

</menu>